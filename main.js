function preload(){

}
function setup(){
    canvas=createCanvas(640, 480);
    canvas.position(110, 250);
}

function draw(){
    circle(30, 30, 20);
describe('white circle with black outline in mid of canvas');
}